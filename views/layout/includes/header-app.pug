header.bg-indigo-600.p-4
    div.container.mx-auto.flex.justify-between.items-center
        a(href="/") 
            h1.text-2xl.text-white.font-extrabold.text-center Bienes
                span.font-normal Raices

        nav(class="my-5 text-sm md:flex md:items-center md:gap-3 font-bold text-white hidden") 
                a(href="/auth/registro") Crear cuenta
                a(href="/auth/login" class="py-3 bg-indigo-700 rounded-lg px-4") Iniciar sesión   

div(class="bg-indigo-700 py-5 hidden lg:block")
    div(class="container mx-auto flex justify-between")
        nav(class="flex gap-4 items-center")
            each categoria in categorias
                a(href=`/categorias/${categoria.id}` class="text-sm font-bold uppercase text-white hover:bg-indigo-500 border-none px-6 py-2 rounded-lg")= categoria.nombre

        form(method="POST" action="/buscador" class="flex gap-5 items-center")
            input(type="hidden" name="_csrf" value= csrfToken)
            label(for="termino" class="text-sm uppercase font-bold text-white") Búsqueda 
            input(type="text" id="termino" name="termino" placeholder="Buscar Propiedades" class="py-2 rounded-lg shadow text-center")
            input(type="submit" class="bg-indigo-800 hover:bg-indigo-400 rounded-lg text-white uppercase font-bold p-2 cursor-pointer text-sm" value="Buscar")